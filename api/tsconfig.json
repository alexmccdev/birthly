{
    "compilerOptions": {
        "baseUrl": "src",
        "typeRoots": ["node_modules/@types"],
        // Should match _moduleAliases in package.json
        "paths": {
            "@controllers/*": ["controllers/*"],
            "@routes/*": ["routes/*"]
        }
    }
}
